# 项目级协作规则

本文件适用于 `E:\数模国赛\校赛` 及其全部子目录，用于让后续进入仓库的 Agent 保持相同的数据口径、阶段顺序和复现标准。用户最新指令、系统/开发者指令以及 2026 年全国大学生数学建模竞赛官方规则优先于本文件。

## 1. 项目目标与当前阶段

- 赛题：A 题“国际油价预测建模”。
- 目标竞赛口径：按 2026 年国赛要求进行校赛模拟。
- 项目根目录：`E:\数模国赛\校赛`。
- 当前数据截止日：`2026-07-29`。
- 当前分支阶段：`DATA_ACQUISITION_AND_AUDIT`，即数据收集与审计。
- 当前不存在最终模型；仓库中的任何模型名称都只能视为候选。
- P0 数据审计通过前，不得宣称模型已经确定，不得生成或填写数值结论。
- 长程目标与阶段完成定义见 `docs/国一目标路线图.md`。
- 2026 官方规则、AI 披露、匿名和提交限制见 `docs/2026国赛规则与合规清单.md`。
- 当前国一目标“可提交准备度”约为 18/100；该数值是工程盘点，不是获奖概率。

## 2. 不变项与暂定项

### 当前不变项

- 必须回答题目全部三个子问题。
- 研究链条为“外部冲突与能源通道冲击—国际油价—中国宏观传导—政策缓冲与跨国比较”。
- 事件必须使用实际发生日；公告日与实施日分开保存。
- 油价峰值、波动率峰值和 GDP 变化属于结果，不能作为因果处理事件。
- 所有外部数据必须记录来源、获取时间、字段定义、许可信息和 SHA-256。
- 所有数值结论必须来自可复现代码的真实运行结果。

### 必须保持暂定的内容

- 最终模型族和主模型/验证模型关系。
- 变量变换、滞后阶数、预测步长、训练窗口和状态数量。
- 跨国对照样本、政策指数权重和缺失值处理策略。
- 事件强度指数的构造方式。
- 论文中的任何定量判断。

上述内容只有在数据审计和小规模验证提供证据后才能冻结。

## 3. 数据优先工作流

严格按以下顺序推进：

1. 登记数据来源、变量、单位、频率和许可。
2. 下载 P0 原始数据并保留原文件。
3. 记录请求参数、UTC 获取时间和文件 SHA-256。
4. 检查缺失、重复、异常、修订、量纲、频率、时间范围和共同样本。
5. 生成数据可用性与审计结论，不进行正式模型拟合。
6. 根据数据特征形成每题不超过两个独立模型体系的候选方案。
7. 以小规模滚动回测、残差诊断和可解释性证据选择最终方案。
8. 完成 `题目分析报告.md` 与 `术语表格.md`，通过 M1 后进入正式编程。
9. 按 P1、P2、W1、W2 门禁依次完成最小结果、全量计算和论文。

禁止为了赶进度跳过数据审计，也禁止在看到最终结果后倒推选择标准。

## 4. P0 数据审计准入

进入模型选择前，至少满足：

- 所有 P0 变量均有明确来源、单位、频率和时间口径。
- 核心序列目标覆盖率原则上不低于 95%。
- Q1 包含充分的战争前训练样本，并完整覆盖 2026 年事件期。
- Q2 月度变量形成足够长的共同时间窗口，季度 GDP 至少覆盖到 `2026Q2`。
- Q3 对照国拥有可比较的共同季度窗口；不同发布时间造成的 ragged edge 已明确记录。
- 缺失值、异常值、季节调整、插值和频率转换均有显式规则；不得静默填补。
- 数据文件哈希、获取时间和许可证/使用条款已经登记。
- 无法获取的变量有替代方案或删除理由，且不会改变题目目标。

具体阈值由 `configs/data_audit.yaml` 管理。阈值变更必须说明数据证据。

## 5. 模型选择规则

模型选择必须同时考虑：

- 与题目目标和机制的匹配程度；
- 数据频率、样本量、平稳性、结构突变和外生性条件；
- 滚动回测或留出集表现；
- 残差、稳定性和不确定性诊断；
- 参数可解释性和政策反事实可实现性；
- 运行成本、复现性和论文表达清晰度。

不得因为模型复杂而优先采用，也不得只按单一误差指标选择。每道子问题最终最多保留两个独立模型体系。

## 6. 数据与文件规范

- `data/raw/`：原始下载文件；大文件或受许可限制文件不提交。
- `data/interim/`：可重新生成的中间文件。
- `data/processed/`：冻结后的建模输入。
- `data/metadata/`：数据字典、来源、时间线、哈希和审计记录。
- `configs/`：可审查的阈值和候选参数，禁止把凭据写入配置。
- `src/`：可运行的数据与模型代码。
- `results/`：真实运行结果和 `复现清单.json`。
- `figures/`：按 `raw_q*_*`、`process_q*_*`、`result_q*_*` 命名。
- 仓库根部 `data/*.csv` 视为队友实时投递区；每次使用前必须锁定远端提交 SHA、文件 Git blob SHA 和 SHA-256，并与官方来源逐值或逐文件核验。
- 模型不得读取浮动的 `main` 数据；只读取已经写入下载清单和审计结果的固定快照。同一路径更新时新增快照记录，不覆盖旧哈希。

API 密钥只允许放在被忽略的 `.env` 中。不得提交令牌、个人凭据、Office 临时文件、缓存和可再生的 LaTeX 辅助文件。

## 7. Git 协作

- `main` 只保存稳定、已验证版本。
- 数据阶段使用 `agent/data-pipeline`；后续阶段使用对应 `agent/...` 分支。
- 提交前检查工作区，只暂存本任务文件。
- 每次提交前至少运行：

```powershell
python -m src.data.validate_metadata
python -m unittest discover -s tests -v
git diff --check
```

- 功能分支默认通过草稿 PR 合并；不得把未经校验的数据或模型直接推到 `main`。

## 8. 阶段变更

只有 P0 数据审计通过后，才能把当前阶段改为 `MODEL_SELECTION`。阶段变更时必须同步更新：

- 本文件的“当前阶段”；
- `README.md` 的项目状态；
- `configs/data_audit.yaml`；
- `configs/model_params.yaml`；
- GitHub PR 中的证据与校验结果。

模型方案冻结后若数据、题目理解或关键假设发生实质变化，必须重新执行受影响的数据审计和 M1 门禁。
